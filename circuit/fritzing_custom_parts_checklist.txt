ORCINY FRITZING CUSTOM-PARTS CHECKLIST
======================================

Goal
----
Replace generic parts in orciny_wiring_starter.fzz with exact (or closest) parts,
then reconnect nets to match the firmware pin map.

Files in this folder
--------------------
- orciny_wiring_starter.fzz
- orciny_fritzing_mapping.txt
- orciny_wiring_ascii.txt
- orciny_wiring.mmd

Primary Target Hardware
-----------------------
1) Adafruit Feather RP2040
2) Adafruit Feather M0
3) 3x momentary switches
4) Direct spark filament outputs plus MOSFET channels for beam RGB, peltier, and pump enable
5) DC pump
6) FP-S14B claw servo
7) 1 core NeoPixel strip (30 pixels)

Part Search Terms (Fritzing Parts Bin)
--------------------------------------
Use these exact search strings in Fritzing's parts search:

RP2040 side
- "Feather RP2040"
- "Adafruit Feather"
- "Raspberry Pi Pico" (fallback footprint reference only)

M0 side
- "Feather M0"
- "Adafruit Feather M0"
- "Feather"

Motor control
- "DC motor"
- "peristaltic pump"

NeoPixel / strip outputs
- "NeoPixel"
- "WS2812"
- "LED strip"
- "RGB LED strip"

Switch and drivers
- "pushbutton"
- "momentary switch"
- "N-MOSFET"
- "logic level MOSFET"
- "relay module"

Power and distribution
- "screw terminal"
- "barrel jack"
- "power connector"
- "ground"

Replacement Mapping (Generic -> Exact)
--------------------------------------
1) Replace generic main controller with: Feather RP2040
2) Replace generic secondary controller with: Feather M0
3) Replace generic motor block with: pump MOSFET + pump connector block
4) Replace generic LED output block with: single NeoPixel strip block
5) Keep three discrete pushbuttons (one per control input)
6) Keep MOSFET channels for beam, peltier, and pump paths only; spark filaments are direct GPIO outputs

Netlist To Verify After Replacement
-----------------------------------
Switches (to ground, INPUT_PULLUP in firmware)
- SW1 -> M0 pin 2 and GND
- SW2 -> M0 pin 3 and GND
- SW3 -> M0 pin 4 and GND

RP2040 PWM / control outputs
- pin 5  -> Spark filament 1 direct path via series resistor
- pin 6  -> Spark filament 2 direct path via series resistor
- pin 9  -> Spark filament 3 direct path via series resistor
- pin 10 -> Spark filament 4 direct path via series resistor
- pin 12 -> Beam R MOSFET gate
- pin 13 -> Beam G MOSFET gate
- pin 14 -> Beam B MOSFET gate
- pin 15 -> Peltier MOSFET/relay input

Inter-controller link
- Feather M0 Serial1 TX -> RP2040 Serial1 RX
- Feather M0 GND -> RP2040 GND

Pump and servo outputs
- RP2040 pin 8 -> pump MOSFET gate -> DC pump
- RP2040 pin 7 -> FP-S14B servo signal

Core LED output
- M0 pin 13 -> NeoPixel strip data in

Power rails
- +5V_BUS -> RP2040, M0, NeoPixel strip, beam, peltier, pump, and servo power
- +3V_FILAMENT -> optional dedicated spark filament power branch
- GND  -> common return for all modules, switches, and drivers

Board-View Completion Steps
---------------------------
1) Open orciny_wiring_starter.fzz.
2) Replace one part at a time from controllers outward.
3) After each replacement, reconnect only the nets for that part.
4) Lock placed parts to avoid accidental movement.
5) Add labels to every critical wire (pin numbers and rail names).
6) Save as: orciny_wiring_custom.fzz.

Schematic-View Sanity Checks
----------------------------
1) Confirm SW1/SW2/SW3 each only connect to one GPIO and GND.
2) Confirm only the four spark filament channels are direct GPIO loads; keep beam, peltier, and pump on switched output stages.
3) Confirm RP2040 TX goes only to M0 RX for serial link.
4) Confirm all grounds are tied together.
5) Confirm +5V_BUS and +3V_FILAMENT domains are not shorted.

Optional Export Targets
-----------------------
- Breadboard view screenshot for assembly
- Schematic view PDF for electrical review
- Net labels printed for wiring harness build
